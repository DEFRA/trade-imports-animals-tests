/**
 * Accompanying documents go to the separate document service and never appear
 * in the notification payload, so this page contributes nothing.
 */
export function accompanyingDocuments(): void {}
