# pages (E2E)

This folder is intended for page tests.

## Notes

- A page test focuses on validating a single page’s behaviour (rendering, interaction, and validation), including individual components or isolated features, rather than a complete user journey across multiple pages.
- This directory may start empty; Git won’t track empty folders, so the presence of this README is intentional until the first spec is added.
